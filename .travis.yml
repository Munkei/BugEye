addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.8
      - g++-4.8
      - clang

compiler:
  - clang
  - gcc

install:
  - '[ "$CXX" = "g++" ] && export CXX="g++-4.8" CC="gcc-4.8"'

language: cpp

os:
  - linux

script: "./continuous.sh"

sudo: false
